import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    alignItems: "center",
    paddingTop: 80,
  },
  logo: {
    height: 150,
    maxWidth: "60%",
    resizeMode: "contain",
  },
  buttons: {
    marginTop: 80,
  },
  button: {
    marginBottom: 24,
  },
});

export default styles;
